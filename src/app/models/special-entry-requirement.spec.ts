import { SpecialEntryRequirement } from './special-entry-requirement';

describe('SpecialEntryRequirement', () => {
  it('should create an instance', () => {
    expect(new SpecialEntryRequirement()).toBeTruthy();
  });
});
