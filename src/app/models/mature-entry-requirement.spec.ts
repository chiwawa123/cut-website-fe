import { MatureEntryRequirement } from './mature-entry-requirement';

describe('MatureEntryRequirement', () => {
  it('should create an instance', () => {
    expect(new MatureEntryRequirement()).toBeTruthy();
  });
});
