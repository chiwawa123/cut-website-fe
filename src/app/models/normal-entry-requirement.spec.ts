import { NormalEntryRequirement } from './normal-entry-requirement';

describe('NormalEntryRequirement', () => {
  it('should create an instance', () => {
    expect(new NormalEntryRequirement()).toBeTruthy();
  });
});
