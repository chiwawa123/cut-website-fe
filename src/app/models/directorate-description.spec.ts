import { DirectorateDescription } from './directorate-description';

describe('DirectorateDescription', () => {
  it('should create an instance', () => {
    expect(new DirectorateDescription()).toBeTruthy();
  });
});
